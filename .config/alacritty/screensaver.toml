# Separate process per monitor — default IPC would fold the second Super+L
# instance into the first window on one screen.
[general]
ipc_socket = false

[colors.primary]
background = "#000000"
foreground = "#ffffff"

[colors.cursor]
cursor = "#ffffff"

[font]
size = 18.0

[window]
opacity = 1.0
