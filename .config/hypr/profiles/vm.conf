# Hyprluggage VM profile overlay
# Same streaming-safe idle policy as desktop (hypridle stays killed via autostart).
# Hardware cursors are unreliable under virt and browser VNC.

cursor {
    no_hardware_cursors = 1
}
